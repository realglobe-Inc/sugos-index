/**
 * Test case for example.
 * Runs with mocha.
 */
'use strict'

const example = require('../lib/example.js')
const assert = require('assert')
const co = require('co')

describe('example', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Example', () => co(function * () {

  }))
})

/* global describe, before, after, it */

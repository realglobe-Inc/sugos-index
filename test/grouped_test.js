/**
 * Test case for grouped.
 * Runs with mocha.
 */
'use strict'

const grouped = require('../lib/grouped.js')
const assert = require('assert')
const co = require('co')

describe('grouped', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Grouped', () => co(function * () {

  }))
})

/* global describe, before, after, it */

/**
 * Test case for helper.
 * Runs with mocha.
 */
'use strict'

const helper = require('../lib/helper.js')
const assert = require('assert')
const co = require('co')

describe('helper', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Helper', () => co(function * () {

  }))
})

/* global describe, before, after, it */

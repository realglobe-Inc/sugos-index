/**
 * Test case for middleware.
 * Runs with mocha.
 */
'use strict'

const middleware = require('../lib/middleware.js')
const assert = require('assert')
const co = require('co')

describe('middleware', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Middleware', () => co(function * () {

  }))
})

/* global describe, before, after, it */
